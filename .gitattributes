# 换行符统一规则
#
# 这个工程同时跑在三种地方：Windows 上开发、Android 电视上运行、群晖 NAS（Linux）上跑后台。
# 换行符搞错的表现很隐蔽：Windows 的 .bat 变 LF 会莫名其妙执行失败，
# Linux 的 .sh 变 CRLF 直接报 "bad interpreter" 但看不出原因。所以在这里钉死。

# 默认：仓库里一律存 LF，检出也用 LF
* text=auto eol=lf

# Windows 批处理必须 CRLF（这就是我们踩过的那个坑）
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell 脚本必须 LF，否则 Linux 上跑不了
*.sh text eol=lf

# 二进制文件不做任何转换
*.jar  binary
*.exe  binary
*.apk  binary
*.zip  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.mp4  binary
*.ttf  binary

# 语言统计：网页资源算代码
*.html linguist-detectable=true
*.css  linguist-detectable=true
*.js   linguist-detectable=true
